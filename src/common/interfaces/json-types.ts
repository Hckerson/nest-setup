/**
 * Typed interfaces for Prisma `Json` / `Json?` fields.
 *
 * Each type here should correspond 1-to-1 with a field marked `Json` in
 * schema.prisma. The starter ships no Json columns; add a model with a Json
 * field and its matching interface here when you need structured JSON.
 *
 * Example:
 *
 *   // schema.prisma
 *   //   model Profile { preferences Json }
 *
 *   export interface ProfilePreferencesJson {
 *     theme: 'light' | 'dark';
 *     newsletter: boolean;
 *   }
 */

export {};
